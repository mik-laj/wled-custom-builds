name: WLED compile

on:
  workflow_dispatch:
    inputs:
      wled_version:
        description: 'WLED Version (e.g. 16.0.0)'
        required: true
        default: '16.0.0'

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v6

    - name: Set up Python
      uses: actions/setup-python@v6
      with:
        python-version: '3.11'

    - name: Install PlatformIO
      run: |
        make install-tools

    - name: Set up Node.js
      uses: actions/setup-node@v6
      with:
        node-version: 24

    - name: Prepare environment
      run: |
        make prepare

    - name: Prepare Binary
      run: |
        make build
        ls -lah build_output/build/

    - name: Prepare Firmware
      run: |
        make firmware
        ls -lah build_output/firmware/

    - name: Create Release
      uses: softprops/action-gh-release@v3
      with:
        tag_name: v${{ inputs.wled_version }}
        name: WLED ${{ inputs.wled_version }} for SP530E

        body: |
          ### Custom Build Details
          - **Version:** ${{ inputs.wled_version }}
          - **Chip:** ESP32-C3
          - **USB CDC:** 0

        files: build_output/firmware/*.bin
        fail_on_unmatched_files: true
